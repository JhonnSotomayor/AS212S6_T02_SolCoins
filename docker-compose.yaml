
version: '2'

services:
  app:
    image: jhonnsotomayor/pib:latest
    container_name: pib
    ports:
      - "4200:4200"
    #environment:
    #  MY_ENVIRONMENT: ${MY_ENVIRONMENT}
    #volumes:
    #  - /ruta/en/el/host:/ruta/en/el/contenedor
    #  sudo lsof -i :4200
